[Unit]
Description=Emacs: the extensible, self-documenting text editor

[Service]
Type=forking
ExecStart={{ emacs_bin }} --daemon
ExecStop={{ emacs_client_bin }} --eval "(kill-emacs)"
Restart=always

[Install]
WantedBy=default.target
